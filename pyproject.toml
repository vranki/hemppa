[project]
name = "hemppa"
packages = [
    {include = "hemppa", from = "."},
]
license = { file = "LICENSE" }
readme = "README.md"
requires-python = ">=3.9,<4.0"
dependencies = [
    "matrix-nio>=0.25.2",
]
